import NextInventoryForm from '../components/inventory/NextInventoryForm';

export default function CreateNextInventoryPage() {
  return (
    <main className='overscroll-auto relative min-h-screen flex flex-col items-center justify-start'>
      <NextInventoryForm />
    </main>
  );
}
